# puppet-moodle/hiera.yaml
---
version: 5
defaults:
  datadir: data
  data_provider: hiera

hierarchy:
  - name: "Per-OS defaults"
    path: "os/%{facts.os.name}.yaml"

  - name: "Common defaults"
    path: "common.yaml"